log:
  file:
    path: C:\Users\admin\IdeaProjects\logs

spring:
  profiles:
    active: prod

server:
  port: 8443
  ssl:
    key-store-type: PKCS12
    key-store: classpath:keystore/devtalk.p12
    key-store-password: 123456
    keyy-alias: devtalk

#HTTP port
http:
  port: 8080
